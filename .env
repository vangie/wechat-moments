# Debug output directory for --debug.
# When set, timestamped run dirs (e.g. 2026-03-14T18-00-00) are created under this path
# instead of ~/.local/share/wechat-moments-poster/debug/
# Use a relative path (e.g. .debug) for project dir, or an absolute path.
# WECHAT_POSTER_DEBUG_DIR=.debug
